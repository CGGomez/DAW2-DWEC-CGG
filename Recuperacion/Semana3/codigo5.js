let x;
let y;

x = prompt("introduce un número para la coordenada X");
y = prompt("introduce un número para la coordenada y");

constructora(x,y);

function constructora (){
    if(isNaN(x)){
        x = 0;
    }
    if(isNaN(y)){
        y = 0;
    }
}

let cambiar = {
    x = prompt("Itroduzca el nuevo valor para la coordenada X"),
    y = prompt("Itroduzca el nuevo valor para la coordenada Y"),

    constructora(x,y);
}

let iguales ={

}

let copia ={

}

let suma ={

}

let distancia ={

}

let toString ={
    
}